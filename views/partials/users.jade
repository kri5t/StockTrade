div(class="row")
    h4 Users
    p There are {{results.length}} users in the system!
    table(class="table table-striped")
        thead
            tr
                th Name
        tbody
            tr(ng-repeat='user in results')
                td
                    a(href='#/getUserByID/{{user.user_id}}') {{user.user_name}}
